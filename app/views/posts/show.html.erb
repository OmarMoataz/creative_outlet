<section class="col-md-2 content"> </section>

<section class="col-md-8">
  <h3 class="title"> <%= @post.title %> </h3>
  <p class="content"> <%= @post.content %> </p>
  <% unless current_user.nil? %>
    <p class="byline"> <%= @post.user.name  %> </p>
  <% end %>
</section>